name: getx_base_classes
description: A collection of base classes for GetX architecture in Flutter, providing common functionality for controllers, views, middleware, and more.
version: 0.0.2
homepage: https://github.com/VisalSuresh90/getx_base_classes.git


environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:


